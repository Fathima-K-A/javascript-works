// convert all objects to uppercase


var arr=["program","project","profile","profession","django","Python","javascript"]

// longest word

var word=arr.filter((w1,w2)=>)

// var upperCaseStrings=arr.map(w=>w.toUpperCase())
// console.log(upperCaseStrings);


// print length of each object

// var object=arr.map(w=>w.length)
// console.log(object);


// return words starting with pro

var wordStart=arr.filter(w=>w.startsWith("pro"))
console.log(wordStart);